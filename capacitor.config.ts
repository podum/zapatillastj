import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.zapatillastj',
  appName: 'zapatillastj',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
